services:
  db:
    image: postgres:latest
    restart: always
    environment:
      - POSTGRES_PASSWORD=123456
      - POSTGRES_DB=futeboxd-api
    env_file:
      - ../.env
    ports:
      - 5432:5432
    volumes:
      - ./init-database:/docker-entrypoint-initdb.d