name: CI do servico de football

on:
  push:
    branches: [ "main" ]
    paths:
        - "football/**"
  pull_request:
    branches: [ "main" ]
    paths:
      - "football/**"

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
          - name: "Checkout repository"
            uses: actions/checkout@v4
          - name: "Set up Go"
            uses: actions/setup-go@v4
            with:
              go-version: '1.23'
          - name: "Run tests"
            run: |
              make -C football/ test
    build:
      runs-on: ubuntu-latest
      needs: test
      steps:
        - name: "Log in to docker hub"
          uses: docker/login-action@v3
          with:
            username: ${{ secrets.DOCKER_USERNAME }}
            password: ${{ secrets.DOCKER_PASSWORD }}
        - name: "Checkout repository"
          uses: actions/checkout@v4
        - name: "Build Docker Image"
          run: |
            make -C football/ docker:build
        - name: "Push Docker Image"
          run: |
            make -C football/ docker:push
